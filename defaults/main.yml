---

loki_version: 2.2.1
loki_config_dir: /etc/loki
loki_db_dir: /var/lib/loki
loki_read_only_dirs: []
loki_config_file: templates/loki.yml.j2

# defaults for the config file that can be changed.

auth: false
server_http_listen_port: 3100
server_grpc_listen_port: 9096
index_retention_period: "24h"

# everything else is as default